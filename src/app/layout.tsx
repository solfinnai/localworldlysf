import type { Metadata } from "next";
import { Playfair_Display, Inter } from "next/font/google";
import "./globals.css";

const playfair = Playfair_Display({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-playfair",
});

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "LocalWorld Spotlight | San Francisco's Local Media Platform",
  description: "Discover the stories behind San Francisco's best local businesses. Interviews, features, and neighborhood guides.",
  openGraph: {
    title: "LocalWorld Spotlight | San Francisco's Local Media Platform",
    description: "Discover the stories behind San Francisco's best local businesses.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${playfair.variable} ${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
